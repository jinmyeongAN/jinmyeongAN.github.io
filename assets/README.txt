Put your headshot here as `profile.jpg` (square crop, at least 600x600).

The homepage shows a dashed placeholder box until that file exists; once you add
it, the photo appears automatically. To use a different filename or format,
update `author.photo` in gatsby-meta-config.js.
